1. Pay attention and follow every instruction in the prblem description and test cases. 
2. Use '.card h4' as a selector to select the entire h4, 
   and under this selector apply the styles required for content in h4 apart from <span>, i.e., '$399'
3. Use '.card h4 span' as a selector to select the span inside of h4,
   and under this selector, apply the respective styles required.